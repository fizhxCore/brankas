name: brankas
description: Vault pribadi untuk menyimpan link, teks, secret, code, dan dokumen.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.3
  flutter_secure_storage: ^9.2.2
  share_plus: ^9.0.0
  file_picker: ^8.0.6
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
